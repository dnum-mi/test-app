name: Clean GitHub Cache and GHCR Image

on:
  pull_request:
    types:
    - closed
    branches:
    - "**"
  workflow_dispatch:
    inputs:
      IMAGE:
        description: Name of the image to delete from ghcr.io (eg. 'my-org/my-image:my-tag')
        required: false
        type: string
      PR_NUMBER:
        description: ID number of the pull request assiocited with the cache
        required: false
        type: number
      BRANCH_NAME:
        description: Branch name assiocited with the cache
        required: false
        type: string

jobs:
  cleanup-cache:
    uses: dnum-mi/fabnum-cicd/.github/workflows/clean-cache.yml@v0
    # A called workflow can never hold more than its caller grants, so this has
    # to be declared here even though clean-cache.yml asks for it too - with the
    # repository default set to read-only, omitting it caps the token at read
    # and the cache deletion comes back 403.
    permissions:
      actions: write
    with:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      BRANCH_NAME: ${{ github.event.pull_request.head.ref }}
      CLEAN_GH_CACHE: true
      CLEAN_GHCR_IMAGE: false
      CLEAN_ORPHANED_GHCR_IMAGE: false

  cleanup-images:
    uses: dnum-mi/fabnum-cicd/.github/workflows/clean-cache.yml@v0
    permissions:
      packages: write
    strategy:
      matrix:
        images:
        - name: server
        - name: client
    with:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      BRANCH_NAME: ${{ github.event.pull_request.head.ref }}
      IMAGE: ${{ format('ghcr.io/{0}/{1}:pr-{2}', github.repository, matrix.images.name, github.event.pull_request.number) }}
      CLEAN_GH_CACHE: false
      CLEAN_GHCR_IMAGE: true
      CLEAN_ORPHANED_GHCR_IMAGE: false

  cleanup-orphaned-images:
    uses: dnum-mi/fabnum-cicd/.github/workflows/clean-cache.yml@v0
    permissions:
      packages: write
    strategy:
      matrix:
        images:
        - name: server
        - name: client
    with:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      BRANCH_NAME: ${{ github.event.pull_request.head.ref }}
      IMAGE: ${{ format('ghcr.io/{0}/{1}:pr-{2}', github.repository, matrix.images.name, github.event.pull_request.number) }}
      CLEAN_GH_CACHE: false
      CLEAN_GHCR_IMAGE: false
      CLEAN_ORPHANED_GHCR_IMAGE: true
