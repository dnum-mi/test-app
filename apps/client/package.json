{
  "name": "client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite --host --port 8080",
    "build": "tsc && vite build",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "@types/bun": "^1.3.10"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
