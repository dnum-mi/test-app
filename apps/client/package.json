{
  "name": "client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite --host --port 8080",
    "build": "tsc && vite build",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.6",
    "@types/bun": "^1.3.3"
  }
}
