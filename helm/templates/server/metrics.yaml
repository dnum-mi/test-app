{{- if .Values.server.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "server" "metrics" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "server")) | nindent 4 }}
  {{- if .Values.server.metrics.service.labels }}
    {{- toYaml .Values.server.metrics.service.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.server.metrics.service.annotations }}
  annotations: {{- toYaml .Values.server.metrics.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.server.metrics.service.type }}
  ports:
  - port: {{ .Values.server.metrics.service.port }}
    targetPort: {{ .Values.server.metrics.service.targetPort }}
    protocol: TCP
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "server") | nindent 4 }}
{{- end }}
