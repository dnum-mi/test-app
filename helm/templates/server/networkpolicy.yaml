{{- if .Values.server.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "server" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "server")) | nindent 4 }}
  {{- if .Values.server.pdb.labels }}
    {{- toYaml .Values.server.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.server.pdb.annotations }}
  annotations: {{- toYaml .Values.server.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  policyTypes: {{- toYaml .Values.server.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.server.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.server.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
