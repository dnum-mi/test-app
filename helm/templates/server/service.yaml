apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "server" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "server")) | nindent 4 }}
spec:
  type: {{ .Values.server.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.server.service.port | default 80 }}
    targetPort: {{ .Values.server.containerPortName | default .Values.server.containerPort | default 8080 }}
    protocol: {{ .Values.server.service.protocol | default "TCP" }}
    {{- if eq .Values.server.service.type "NodePort" }}
    nodePort: {{ .Values.server.service.nodePort }}
    {{- end }}
    name: {{ .Values.server.service.portName | default "http" }}
  {{- if .Values.server.service.extraPorts -}}
  {{- toYaml .Values.server.service.extraPorts | nindent 2 -}}
  {{- end }}
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "server") | nindent 4 }}
