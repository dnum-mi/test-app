{{- if and .Values.server.serviceAccount.enabled .Values.server.serviceAccount.clusterRole.create .Values.server.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "server" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "server")) | nindent 4 }}
rules: {{- toYaml .Values.server.serviceAccount.clusterRole.rules | nindent 0 }}
{{- end }}
