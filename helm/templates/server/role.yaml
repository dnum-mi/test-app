{{- if and .Values.server.serviceAccount.enabled .Values.server.serviceAccount.role.create .Values.server.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "server" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "server")) | nindent 4 }}
rules: {{- toYaml .Values.server.serviceAccount.role.rules | nindent 0 }}
{{- end }}
