{{- if and .Values.server.serviceAccount.enabled .Values.server.serviceAccount.clusterRole.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "server" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "server")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ printf "%s-%s" (include "helper.fullname" .) "server" }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.server.serviceAccount.name | default (printf "%s-%s" (include "helper.fullname" .) "server") }}
  namespace: {{ .Release.Namespace }}
{{- end }}
