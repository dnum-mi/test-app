{{- if .Values.client.ingress.enabled -}}
{{- $fullNameApp := printf "%s-%s" (include "helper.fullname" .) "client" -}}
{{- $svcPortApp := .Values.client.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "client" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "client")) | nindent 4 }}
  {{- if .Values.client.ingress.labels }}
    {{- toYaml .Values.client.ingress.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.client.ingress.annotations }}
  annotations:
    {{- tpl (toYaml .Values.client.ingress.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.client.ingress.className }}
  ingressClassName: {{ .Values.client.ingress.className }}
  {{- end }}
  {{- if gt (len .Values.client.ingress.tls) 0 }}
  tls:
  {{- range .Values.client.ingress.tls }}
  - hosts:
    {{- range .hosts }}
    - {{ . | quote }}
    {{- end }}
    secretName: {{ .secretName | quote | default (printf "%s-%s" $fullNameApp "tls") }}
  {{- end }}
  {{- end }}
  rules:
  {{- range .Values.client.ingress.hosts }}
  - host: {{ .name | quote }}
    http:
      paths:
      - path: {{ .path | default "/" }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
          {{- if .backend }}
            name: {{ .backend.serviceName | default $fullNameApp }}
            port:
              number: {{ .backend.portNumber | default $svcPortApp }}
          {{- else }}
            name: {{ $fullNameApp }}
            port:
              number: 80
          {{- end }}
  {{- end }}
{{- end }}
