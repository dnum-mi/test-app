{{- if and .Values.client.serviceAccount.enabled .Values.client.serviceAccount.clusterRole.create .Values.client.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "client" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "client")) | nindent 4 }}
rules: {{- toYaml .Values.client.serviceAccount.clusterRole.rules | nindent 0 }}
{{- end }}
