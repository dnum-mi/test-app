{{- if .Values.client.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "client" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "client")) | nindent 4 }}
  {{- if .Values.client.pdb.labels }}
    {{- toYaml .Values.client.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.client.pdb.annotations }}
  annotations: {{- toYaml .Values.client.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  policyTypes: {{- toYaml .Values.client.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.client.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.client.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
