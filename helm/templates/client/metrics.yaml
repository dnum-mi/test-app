{{- if .Values.client.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "client" "metrics" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "client")) | nindent 4 }}
  {{- if .Values.client.metrics.service.labels }}
    {{- toYaml .Values.client.metrics.service.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.client.metrics.service.annotations }}
  annotations: {{- toYaml .Values.client.metrics.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.client.metrics.service.type }}
  ports:
  - port: {{ .Values.client.metrics.service.port }}
    targetPort: {{ .Values.client.metrics.service.targetPort }}
    protocol: TCP
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "client") | nindent 4 }}
{{- end }}
