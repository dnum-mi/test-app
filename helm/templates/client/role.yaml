{{- if and .Values.client.serviceAccount.enabled .Values.client.serviceAccount.role.create .Values.client.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "client" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "client")) | nindent 4 }}
rules: {{- toYaml .Values.client.serviceAccount.role.rules | nindent 0 }}
{{- end }}
